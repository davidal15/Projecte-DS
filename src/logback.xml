<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console output -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Activity log: ALL requests must go here -->
    <appender name="ACTIVITY" class="ch.qos.logback.core.FileAppender">
        <file>activity.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ========================================================== -->
    <!--                  FITA 2 — REQUEST LOGGING                  -->
    <!-- ========================================================== -->

    <!-- Only log milestone 2 classes (WebServer + Requests) -->
    <logger name="baseNoStates.requests" level="INFO" additivity="false">
        <appender-ref ref="ACTIVITY" />
    </logger>

    <logger name="baseNoStates.WebServer" level="INFO" additivity="false">
        <appender-ref ref="ACTIVITY" />
    </logger>

    <!-- Root logger (only errors to console + file) -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ACTIVITY" />
    </root>

</configuration>
